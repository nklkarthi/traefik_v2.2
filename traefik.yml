## traefik.yml

entryPoints:
  web:
    address: ":8081"
  websecure:
    address: ":8443"

log:
  level: DEBUG

# File configuration backend
providers:
  file:
    filename: /etc/traefik/traefik.yml

tls:
  stores:
    default:
      defaultCertificate:
        certFile: /var/www/traefik/certs/fullchain.pem
        keyFile: /var/www/traefik/certs/privkey.pem

api:
  insecure: true
