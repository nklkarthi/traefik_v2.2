http:
  routers:
    minio:
      rule: "PathPrefix(`/gitlab`)"
      entryPoints:
        - web
      service: gitlab

  services:
    gitlab:
      loadBalancer:
        servers:
          - url: "https://centos-vm2.eastus2.cloudapp.azure.com/users/sign_in/"
