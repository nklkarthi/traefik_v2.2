http:
  routers:
    minio:
      rule: "PathPrefix(`/minio`)"
      entryPoints:
        - websecure
      service: minio
      tls: {}

  services:
    minio:
      loadBalancer:
        servers:
          - url: "https://centos-vm2.eastus2.cloudapp.azure.com:9199/minio/"
